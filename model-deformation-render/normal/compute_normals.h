#ifndef _compute_normals
#define _compute_normals

#include <vector>

std::vector< std::vector<double> > compute_normals(std::vector< std::vector<double> > my_vertices, std::vector< std::vector<int> > my_faces);

#endif
